services:
  portainer:
    container_name: portainer
    image: portainer/portainer-ce:2.21.0
    # ports:
    #   - 8000:8000
    #   - 9443:9443
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock
      - ${PORTAINER_VOLUME}/portainer_data:/data
    restart: always
